#include <stdio.h>
#include <stdlib-h>
#include <time.h>
/**
 * main - Imprimir numeros
 * Return: 0 for success
 */
int main(void)
{
char N = '0';

for (N = '0'; N <= '9'; ch++)
putchar(N);
putchar('\n');
return (0);
}
